{
  "name": "benzo-console-workspace",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm --filter @benzo/console dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "typescript": "^5.5.0"
  }
}
